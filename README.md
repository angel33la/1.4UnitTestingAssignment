# 1.4 Unit Testing Assignment

This repository contains a small Node.js unit testing assignment with two modules:

- `total.js` — calculates the total of an array of prices
- `obscureCreditCard.js` — masks a credit card number except for the last 4 digits, and returns `invalid credit card` for invalid lengths or non-string input

## Files

- `package.json` — project metadata and test script
- `total.js` — total calculation module
- `obscureCreditCard.js` — credit card obscuring module
- `total.test.js` — Jest tests for both modules
- `.gitignore` — ignore build and OS files

## Setup

```bash
npm install
```

## Run tests

```bash
npm test
```

## GitHub

Published repository: https://github.com/angel33la/1.4UnitTestingAssignment
